<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Nueva cotización de Seguros</title>
  
  <style>
    .quotation-table {
      text-align: center;
    }

    .head-cell {
      font-size: 0.9rem;
      padding: 10px 5px;
      background-color: #808080;
      color: #fff;
      font-weight: bold;
    }

    .cell {
      text-align: center;
      font-size: 0.85rem;
      padding: 0 10px;
    }
  </style>
</head>
<body>
  <table class="quotation-table">
    <thead class="head">
      <tr>
        {{#each headersTable}}
          <th class="head-cell">{{this}}</th>
        {{/each}}
      </tr>
    </thead>
    <tbody class="body">
      <tr>
        {{#each cellsTable}}
          <td class="cell">{{this}}</td>
        {{/each}}
      </tr>
    </tbody>
  </table>
</body>
</html>